# Poort 5b sanity-check rapport — TEAM_ABONNEMENT_01
delivery: "Sparki Team-abonnement: tier TEAM (€149/mnd, €1.490/jr), club-checkout eigenaar-only, webhook-sync clubabonnement, rollen soigneur/medic, Team-paneel in club-beheer"
date: "2026-07-31"
checked_by: "replit"
surfaces:
  - "Club-beheer (web): Sparki Team-paneel — status, prijzen, checkout-knoppen (eigenaar) en eerlijke uitleg voor niet-eigenaren"
  - "API: POST /api/clubs/:id/team-subscription/checkout en GET /api/clubs/:id/team-subscription (testmatrix 7/7)"
  - "Stripe-webhookpad: activering/pauze/herstel van club_subscriptions via gesigneerde events (testmatrix)"

checks:
  dead_controls:
    cases:
      - case: "Checkout-knoppen 'Start €149/maand' en 'Start €1.490/jaar' roepen de checkout-route aan; server-side 403 voor niet-eigenaar en 400 voor ongeldig interval automatisch getest (scenario 2)"
        result: "pass"
      - case: "Zonder opengestelde betaalomgeving toont het paneel geen dode knop maar de eerlijke melding 'Afsluiten kan zodra de betaalomgeving voor dit account is opengesteld'"
        result: "pass"
  context_nonsense:
    cases:
      - case: "Niet-eigenaren zien geen checkout-knoppen maar 'Alleen de eigenaar kan dit abonnement afsluiten' — geen aanbod dat ze niet mogen gebruiken"
        result: "pass"
      - case: "Persoonlijke billing-checkout weigert tier TEAM (400): een team zonder organisatie is contextonzin en wordt server-side geblokkeerd"
        result: "pass"
  placeholder_as_result:
    cases:
      - case: "Team-paneel rendert niets tot echte serverdata binnen is (geen skeleton dat blijft staan); status/limieten/periode-einde komen uitsluitend uit server-geresolvede staat, geblokkeerde betaling toont eerlijke uitleg i.p.v. neptekst"
        result: "pass"

verdict: "deliverable"
