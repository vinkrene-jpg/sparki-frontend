# Sparki — Moduledetails (verplaatst uit replit.md)

Gedetailleerde per-module documentatie. De compacte index staat in `replit.md` (sectie "Product"). Bij wijzigingen aan een module: werk dít bestand bij, niet replit.md.

## Product

- **Landing page** (`/`) — public, unauthenticated users see branded Sparki landing with sign-in/sign-up CTAs. Authenticated users see the Training Day Home directly.
- **Sign-in / Sign-up** (`/sign-in`, `/sign-up`) — custom Clerk-themed pages matching the dark Sparki design language (dark bg, cyan accent, Inter Variable font, Sparki bolt logo).
- **Protected app routes** (`/train`, `/feed`, `/lab`, `/you`) — redirect to `/sign-in` when not authenticated.
- **Role switcher** — in the `ScreenShell` header for users with multiple roles; cycles through `athlete`/`coach`/`parent`. Sign-out button alongside.
- **Cinematic background** — lives in `ScreenShell` (shared by Home/Train/Feed/Lab/You). A `fixed` layer keeps the cyclist (`/concept-lab.png`) visible behind the whole page with subtle parallax: image ~0.56 opacity, softened blue-black gradient overlay, drifting atmospheric haze, bottom vignette for nav legibility, OLED-safe base `#05070e`. Text over the background uses soft dark scrims (not boxes). Cards are frosted glass `bg-[#070d16]/[0.82] backdrop-blur-md` with subtle light borders so the background shows through (~18%).
- **Admin & gezondheidscheck** (`/admin`, `/admin/health/:checkKey`) — admin-only. An automated Health Check engine probes every dependency for real (auth, database, Sparki-denkkracht, routes/kaarten, connectors, meldingen, onboarding, uitnodigingen, ouder-toezicht, koppelingen, feedback, bugmeldingen, nachtelijke scan, maandelijkse doelen-review) and shows four honest statuses: groen (werkt) / oranje (let op) / rood (storing) / grijs (nog niet gekoppeld). Nothing is ever a fake green — unwired capabilities (e-mail, GPS, GPX-export, bestandsopslag, niet-beschikbare connectors) are GREY with a plain-language reason and detect presence rather than assume. Each failure has user-impact + remediation + a detail page with history, "Opnieuw testen" and "Markeer als opgelost". "Controleer nu" runs the full engine on demand. A CLI job (`pnpm --filter @workspace/api-server run job:health`, modes `daily`/`weekly`/`release` via `HEALTH_CHECK_MODE`) backs Scheduled Deployments; `release` mode exits non-zero on unresolved red failures (pre-release gate).
- **Development Preview Mode** — dev-only. When the Vite dev server runs (`import.meta.env.DEV`), `App.tsx` renders `<DevPreview>` instead of the auth-gated router, mounting the exact production components (no duplication) with a fixed dev switcher bar (Landing/Home/Train/Feed/Lab/You) driven by wouter location. Backend pairs this with `devAuthBypass` middleware that resolves a dev user when no real Clerk session exists. Fully disabled in production: frontend branch is dead-code in prod builds; backend bypass requires BOTH `NODE_ENV !== "production"` AND `DEV_AUTH_BYPASS=true` (fails closed otherwise).
- **Vraag Sparki chat** (`sparki-chat-overlay.tsx` + `sparki-input-center.tsx`) — the chat opens from the **SPARKI mark in the ScreenShell header** (blue-dot wordmark, every screen → a button), as a portal overlay (z-[80], above the bottom nav; X/backdrop/Escape close, body-scroll lock). Composer is two rows: row 1 = text input + send, row 2 = attachment options (foto/bestand/link). The **visible thread is session-scoped**: only turns from the current app-open are shown (module-level `SESSION_START` filters `createdAt >= SESSION_START`); the full history stays in the DB (Sparki's memory) for privacy-gated analysis and is never deleted to achieve a "fresh start". Newest turn stays in view (bottom anchor). It lives ONLY in the header overlay (removed from the Nieuws/Feed page).
- **Training toevoegen (opgesplitst)** — `add-training.tsx`: keuzescherm eerst (Training inplannen / Uitgevoerde training registreren / Trainingsblok maken). Plan-flow (toekomst, plan-velden → `planned_workouts.plan_details` jsonb + `routeId`, server-whitelist `lib/plan-details.ts` weigert ervaring-velden met 400) vs log-flow (verleden, uitgevoerde velden, eerlijke auto-koppel/import-hint). Labels: "Fietstraining", 8 fietsdisciplines, "Crosstraining — hardlopen/zwemmen" (DB-waarden Engels). Dagcontext via `contextDate` (DayDetailDrawer): toekomst→plan, verleden→log (`lib/add-training-flow.ts`, `chooseInitialMode`). Tests: `test:add-training-flow` (sparki) + `test:plan-details` (api-server), via shell.
- **Smart Missing Input Flow** — app-wide rule: a missing-data/empty-state message is NEVER a dead-end. Every empty-state explains what's missing in plain Dutch, offers ≥1 action button to the exact input flow, returns the user to where they came from, and retries the original action. A single registry (`lib/missing-input.ts`, `INPUT_TARGETS`) is the source of truth for each input target (label, route, `?focus` token, plain-Dutch why, and a `isSet(profile)` presence check). Navigation hooks (`hooks/use-missing-input.ts`): `useStartFix()` navigates `route?focus=&returnTo=&retry=`; `useCompleteFix()` returns to `returnTo?retry=`; `useRetryAction(key, fn)` fires the original action once on `?retry` match then strips the param. `components/sparki/missing-input-notice.tsx` is the reusable notice (targets→buttons, plus optional `actions`/`primary`); `ftp-estimate-wizard.tsx` lets users estimate FTP when they don't know it (saves `ftp` + `ftpEstimated=true`). `/you` reads `?focus` to auto-open + scroll + highlight the matching editor (ftp/weeklyHours/weight/sportProfile/goal/checkin/connections) and calls `completeFix()` on save. Flagship "Nog geen schema" lives in `three-week-plan.tsx` (canBuild gate; "Ik weet mijn FTP niet" → wizard; auto-runs generate on return). Wired across `/train`, `/lab`, `/races`, `/you` (≥15 situations). **Gotcha:** never pass a `targets` set that can be fully satisfied without also supplying `actions`/`primary` — when every target's `isSet` is true the notice renders zero buttons (a dead-end). `/train`'s "no workout" state branches on `missingTargets(...)`: incomplete profile → target buttons; complete profile → primary "Bouw mijn plan" that scrolls to `#three-week-plan`.

- **Wedstrijdintelligence & technische gids** — één centraal wedstrijdpuntenmodel (`race_points`: kinds start/…/finish + info-kinds, pointClass info|wedstrijd, status voorgesteld|bevestigd|aangepast|afgewezen; nooit terug naar "voorgesteld"). Gids-upload (documentanalyse) levert alleen VOORSTELLEN mét bron/pagina/betrouwbaarheid; kaartcontrole in het RaceForm-paneel (`race-points-panel.tsx` + `use-race-points.ts`, Leaflet-kaartklik snapt km deterministisch via `snapToRouteKm`; handmatig punt = direct bevestigd, zonder bron). `races.localLaps` (int 0–99 anders null) + `assignment`; `routes.usageType="wedstrijd"` laat `GET /api/routes/:id` een `race`-blok meesturen met UITSLUITEND actieve (bevestigd/aangepast) punten. Mobiel wedstrijdmodus (`lib/race-mode.ts` + navigate): rondeteller via wrap-detectie, finish-cue alleen in de laatste ronde, POIs/verkeerslicht onderdrukt. Engine `artifacts/api-server/src/lib/race-points/`, routes `routes/race-points.ts` (ownership via race). Tests: `test:race-points` (9, via shell) + mobiel `race-mode.test.ts` (11, via node --test).
- **Wedstrijdexport naar fietscomputers** — per-wedstrijd exportcentrum in het RaceForm-paneel (`race-export-center.tsx` + `use-race-exports.ts`): GPX (waypoints), Garmin FIT Course (bevestigde/aangepaste punten als course points; dep-vrije encoder `lib/fit-encode.ts`) en FIT Workout (ALLEEN bij echte warming-up ≥5 min of gekoppelde training — nooit verzonnen stappen). Validatie vooraf (§8: start+finish verplicht, unieke finish, punten op route ≤250 m, geen dubbelen, ronde-sanity) blokkeert de export; elk bestand wordt teruggelezen (round-trip §9, CRC + type + aantallen) vóór vrijgave. Historie in `race_exports` (versie `naam_datum_type_vN`, validatie + round-trip vastgelegd); download bouwt altijd deterministisch opnieuw. Nieuwe technische gids → `guide-diff.ts` markeert gewijzigde/verdwenen actieve punten `needsReconfirm` (nooit automatisch verplaatst) en zet exports op "verouderd"; herbevestigen via race-points PATCH wist de vlag. Wahoo/Karoo: alleen eerlijke uitleg, geen sync-knop. Engine `artifacts/api-server/src/lib/race-export/`, routes `routes/race-exports.ts`. Test: `test:race-export` (17 scenario's, via shell).

- **Volgauto (Opdracht 3)** — route-instelling "Volgauto" (standaard uit, `routes.volgautoEnabled`). Bij inschakelen berekent de engine (`artifacts/api-server/src/lib/volgauto/{plan,compute}.ts`, ORS driving-car) een APARTE autoroute — de fietsroute blijft onaangetast — plus gedeeld/gescheiden-segmenten (`kind:"gedeeld"|"gescheiden"`, `startKm`/`endKm` op de fietsroute), aansluitpunten (`name`/`source:"parkeerplaats"|"route"`; echte OSM-parkings ≤400 m via Overpass, `carKm` nullable) en eerlijke `dataNotes`. Plan bevat `routeVersion`; GET rekent `outdated` uit t.o.v. de actuele routeversie. Routes `routes/volgauto.ts` (`/api/routes/:id/volgauto`, + positions/reports); vaste veiligheidsdisclaimer. Web: `use-volgauto.ts` + `volgauto-panel.tsx` in het routepaneel (toggle, vergelijking fiets↔auto, aansluitpunten, disclaimer). Mobiel: rolkeuze bij nav-start (renner/volgauto), `VolgautoDriverMode.tsx` (automodus), `lib/volgauto-meet.ts` (stabiele aansluitpuntwissel: pas na 120 s aanhoudende voorkeur, gepasseerd punt direct; ETA's altijd "geschat", positie >3 min = eerlijk geen ETA), na-rit meldingen (DB-enum weg_afgesloten/verboden_voor_autos/niet_praktisch/wachtpunt_ongeschikt — als gebruikersmelding behandeld, geen universele waarheid). Tests: `test:volgauto` (api-server, 18, via shell) + `test:volgauto-meet` (mobiel, 10).
- **Wegtypen & ondergrond + fietsgeschiktheid** — bewaarde routes én bouwer-kandidaten tonen een verdeling over 10 wegtypecategorieën (asfalt, verhard fietspad, klinkers, kasseien, compact/los gravel, onverhard, bospad, singletrack, onbekend) uit ECHTE OSM-tags (Overpass; ODbL-bron zichtbaar). Engine `artifacts/api-server/src/lib/route-surfaces.ts` (classifySurfaceTags: surface/tracktype/mtb:scale, pad/fietspad/weg zonder surface = eerlijk "onbekend" — nooit verzonnen; aggregateSurfaces pure: way-match ≤25 m, km-toewijzing, aaneengesloten segmenten met route-indexen; computeBikeSuitability: racefiets/gravelbike/mountainbike → goed/gedeeltelijk/technisch/afgeraden/onvoldoende_gegevens mét transparante redenen, >40% onbekend ⇒ onvoldoende_gegevens). Endpoints `GET /api/routes/:id/surfaces` (ownership, eerlijke 502) + `POST /api/routes/surfaces-preview` (vóór `/:id`). Frontend `route-surfaces.tsx` + `use-route-surfaces.ts` (paneel met verdelingsbalk; tik op wegtype licht de routegedeelten op de kaart op via `RouteMap.highlightPaths`). Test: `test:route-surfaces` (24 scenario's, via shell).
- **Routeopmerkingen + interactief hoogteprofiel** — bewaarde routes tonen automatische routeopmerkingen uit ECHTE OpenStreetMap-gegevens (via Overpass API; bron/licentie ODbL 1.0 — © OpenStreetMap-bijdragers, altijd zichtbaar in de UI met de kanttekening dat kaartgegevens verouderd kunnen zijn). Engine `artifacts/api-server/src/lib/route-remarks.ts` (deterministische tag-classificatie: veerpont, trap, voorde, poort/afsluiting, fietsverbod, beperkte toegang, natuurgebied, slecht/onverhard wegdek, kasseien; `uncertain` markeert indicaties; HTML in OSM-namen gestript; Overpass-elementen zonder/met null-geometrie eerlijk overgeslagen; nooit verzonnen waarschuwing op een gewone weg). Endpoint `GET /api/routes/:id/remarks` (ownership-check, 15-min in-memory cache, eerlijke `dataRemarks` bij ontbrekende gegevens). Frontend: `route-remarks.tsx` + `use-route-remarks.ts` in het routepaneel ("Toon op kaart" focust de kaart), interactief hoogteprofiel in `elevation-profile.tsx` (`InteractiveElevationProfile`: sleep/schuif ↔ kaartpositie-sync via `positionKm`/`focusPoint` op `RouteMap`, gradiëntkleuren, S/F/klim-markers, opmerkingmarkers op het profiel). Test: `test:route-remarks` (16 scenario's, via shell — workflowlimiet).
- **Wedstrijdkalender-import** (`/races` → "Uit kalender") — instead of typing every race by hand, athletes import from external calendars matched to their sport. Backend engine in `artifacts/api-server/src/lib/calendar/` (dependency-free regex parsers; honest empty/failure, never fabricated): **Fietssport** (fietssport.nl/toertochten — full, exact date resolved on select via detail `<title>`), **We-Tri** (we-tri.nl/competition — full, exact dates in the table), **KNWU** (knwu.nl/kalender — `status: "limited"`: only the ~5 server-rendered "Komende wedstrijden" are readable; the full calendar + personal inschrijvingen live in the `mijn.knwu.nl` SPA which exposes no reachable API/login, so it is NOT faked and carries a plain-Dutch note). Routes (`routes/calendar.ts`, `requireAuth`): `GET /api/calendar/sources` (+ `recommended` from athlete `sport`/`discipline`), `/search?source&q&type&from&to&limit` (30-min in-memory cache), `/event?source&url` (date/GPX enrich, SSRF host-allowlist in `lib/calendar/html.ts`). Frontend: `hooks/use-calendar.ts`, `components/sparki/import-from-calendar.tsx` (source pills defaulted to recommended, debounced search, real results, honest empty/error/limited states), `lib/calendar-types.ts`. Picking an event prefills `EMPTY_FORM` (name/raceDate/location/discipline/distanceKm + a "Geïmporteerd uit …" note) then opens the existing RaceForm for review before save — no auto-write.
- **Voeding-seizoensdoel (17+)** — in de Voeding-sheet stelt een volwassen renner (≥17; jonger krijgt bewust GEEN gewichtssturing, RED-S) seizoensstart, piekdatum en streefgewicht in. `GET/PUT /api/nutrition/season-goal`; Sparki stelt één gerichte vraag tegelijk (ladder: seizoensstart→piek→huidig gewicht→streefgewicht; huidig gewicht schrijft naar `athlete_profiles.weightKg` als SSOT). Deterministische sturing: max 0,5 kg/week, eerlijke waarschuwing bij onhaalbaar tempo. `seasonGoalPromptBlock` injecteert het doel in de fueling-plan- én dag-analyse-prompts met de harde regel: training altijd volledig gevoed, nooit crashdiëten.
- **Voedingsflow (Opdracht 17)** — deterministische rekenkern `lib/fueling.ts` (`computeSessionFuelTargets`): richtwaarden koolhydraten/vocht/natrium per duur/intensiteit/warmte; jeugd (<16) krijgt bewust GEEN getallen (RED-S), alleen gewoonte-advies; coachinstructies letterlijk en bovenaan; eerlijke gaten (geen gewicht ⇒ geen pre/herstel-getal). LLM-fuelingplan krijgt de berekende richtwaarden als leidend blok in de prompt (prose rekent nooit zelf). Routes: `GET/PUT /api/nutrition/preferences` (consent fail-closed via `consentAt`), `GET /api/nutrition/session-targets`; daganalyse vergelijkt plan↔registratie (`compareFuelPlanToLogs`, per-uur-verdicts, GI/energie-signaal); logs hebben `energyFeel` 1–5. Mobiel: bidon/eetmoment-tikkers tijdens de rit; teller én ritdatum worden bij STOPPEN gesnapshot en na succesvol opslaan als één voedingslog gePOST. Test: `test:fueling` (16 scenario's, via shell).
- **Verse beleving per bezoek (presentatie-variatie)** — at every app-open the athlete sees the same REAL analyses/feeds but freshly presented: a different real insight leads and the order varies. A per-app-open `SESSION_ID` (`artifacts/sparki/src/lib/session.ts`) is sent via the `X-Sparki-Session` header in `apiFetch`; `artifacts/api-server/src/lib/variation.ts` (`sessionSeed` → hash header, fallback to calendar day; `seededRotate`/`seededShuffle`/`windowedReorder`/`rotateWithinGroups`) drives a **presentation-only** reorder. Applied to the coach analysis (`composeCoachAnalysis` rotates observations WITHIN their severity tier — `urgent→important→info` — before composing; `variationSeed` truthy only, so jobs/tests are a no-op), `GET /api/ai/observations`, `GET /api/feed/news` (windowed so relevance stays near the top), and `GET /api/social/circle-feed` (follow-ups stay pinned, rotated among themselves). **Honesty:** numbers, conclusions and persistence never change; urgent/important is never demoted below info. Wording already varies because `POST /api/ai/brief` regenerates its prose every call (the daily dedupeKey only writes to memory).

- **Uitleglaag (info-icoontjes)** — technische termen/grafieken dragen een `UitlegDot` (components/viz/uitleg.tsx) die een toegankelijke dialog opent met tabs Wat/Waarom/Hoe plus een eerlijke persoonlijke "Bij jou"-sectie. Bron is één centraal registry `lib/uitleg-content.ts` (per key wat/waarom/hoe + versie; `buildUitlegContextRegels` gebruikt alleen echte profiel/load-waarden, nooit verzonnen). Bare "TSS"-labels heten in de UI "Belasting (TSS)". Test: `test:uitleg-content` (script, via shell draaien — workflowlimiet).

- **Centraal humorniveau (Sparki-stijl)** — instelling in Jij → Instellingen → "Sparki-stijl" (Uit/Subtiel/Normaal/Uitgesproken, default Normaal; `ai_preferences.humor_level`, GET/PUT `/api/ai/preferences`). Eén centrale humorlaag: frontend `lib/humor.ts` (contextpools, cumulatief, anti-herhaling) + `useHumorLine`/`<HumorLine>`; server voice-engine `isToneAvailable(tone, tier, humorLevel)` — "uit" blokkeert dry_humor/cynical op elk vertrouwensniveau, ook in de `/api/voice` stijlvoorbeelden. Humor NOOIT op medisch/veiligheid/privacy/fouten/betalingen. Tests: `test:humor` (frontend) + humorniveau-scenario's in `test:voice`.

- **Persoonlijke Journey + wedstrijddossier** (`/journey`, `/journey/wedstrijd/:raceId`) — chronologische tijdlijn samengesteld op leesmoment uit bestaande tabellen (wedstrijden, sessies, doelen, records uit powerBests, materiaal) + drie eigen tabellen (`journey_items` handmatige mijlpalen/trainingskampen/blessure-herstel, `journey_media`, `journey_reflections`). Wedstrijddossier: basisgegevens, aanloop (14d taper), gekoppelde activiteit (auto = langste sessie op de wedstrijddag; handmatig corrigeerbaar; "geen activiteit" mogelijk), media (foto's/video's, onderschrift, volgorde, zichtbaarheid standaard privé), terugblik (terugblik/les/vervolgactie, upsert), deelkaart. **Privacyregels hard in de API:** minderjarig of onbekende leeftijd ⇒ media kan nooit op "gedeeld" (403, fail-closed); de deelkaart bevat uitsluitend door de renner gekozen velden (whitelist, lege selectie = 400) en weigert privé-media server-side (400). Ingangen vanuit Wedstrijden, Start, Activiteiten, Profiel en World. Engine `artifacts/api-server/src/lib/journey.ts`, routes `routes/journey.ts` (`/api/journey`), frontend `pages/journey.tsx` + `hooks/use-journey.ts`. Test: `test:journey` (18 scenario's).

- **Coachomgeving (cockpit)** (`/coach`, coach-rol) — dashboard met geprioriteerde signalen per sporter (engine `lib/coach-signals.ts`), planning-CRUD voor één of meerdere sporters (bulk), berichten coach↔sporter (accepted link volstaat, ook bij sharing "none"), context-items (transparant voor de sporter) en audit. **Sparki overschrijft coachtrainingen nooit zelf**: sporterfeedback wordt een rij in `coach_change_proposals` (race-veilig via partiële unique index `workout_id WHERE status='open'` + onConflictDoNothing); alleen een expliciet coachbesluit past de training aan. **Cross-coach isolatie:** `planned_workouts.coach_clerk_id` registreert de makende coach; wijzigen/herhalen/voorstel-besluit alleen door de eigenaar (null = legacy, elke gekoppelde coach). Routes `routes/coach-cockpit.ts`, schema `lib/db/src/schema/coach-cockpit.ts`, frontend `pages/coach-cockpit.tsx` + `coach-home.tsx` + `hooks/use-coach-cockpit.ts`. Test: `test:coach-cockpit` (19 scenario's).

- **Mechanieker & materiaalkring** (`/mechanieker`, garage) — multi-fiets beheer met eerlijk afgeleid gebruik: km/uren per fiets/component komen ALTIJD live uit gekoppelde `training_sessions.bike_id` (nooit een teller; sessie weg = km weg). Auto-koppeling gokt nooit (alleen Strava gear_id of precies één actieve fiets; handmatige (ont)koppeling wint altijd, `bike_link_source`). Onderhoudssignalen (`lib/maintenance-signals.ts`): controleadvies / vermoedelijke slijtage (km-drempels per categorie) / vastgesteld defect — defect ALLEEN uit eigen registratie, nooit uit foto's of km; nul gebruiksdata = nul signalen. Vandaag toont compact paneel dat controleadviezen wegfiltert. Materiaalkeuze per wedstrijd/training (`equipment_choices`, atomische upsert via partiële unieke indexen + `onConflictDoUpdate` met `targetWhere`). Scanweergave heet eerlijk "Interactieve fotoweergave"; alleen het parametrische model heet 3D. Engine `lib/bike-usage.ts`, routes `routes/garage.ts`, frontend `bike-garage.tsx`/`equipment-choice.tsx`/`maintenance-signals.tsx`. Test: `test:mechanieker` (17 scenario's).

- **Automatische datasync (Garmin/Wahoo + webhooks)** — Data Hub-uitbreiding: Garmin/Wahoo als volwaardige OAuth-providers (`lib/connectors/providers/device-sync.ts`; eerlijk "niet beschikbaar" zonder fabrikantsleutels). Per-veld herkomst op sessies (`training_sessions.fieldSources` — eerste bron houdt het veld) en heilige handmatige correcties (`manualFields` — nooit door een sync gevuld of overschreven, ook niet als leeg). Webhooks (`engines/data-hub/webhooks.ts`, `routes/webhooks.ts`, `/api/webhooks/*`, geen auth-middleware): idempotent via unieke provider+eventId (`webhook_events`, statussen received/processed/skipped/failed + attempts/lastError), verwerking = gewone `runSync` trigger "webhook". Verificatie: Strava verify-token handshake, Wahoo `webhook_token`, Garmin `?token=GARMIN_WEBHOOK_TOKEN` in de geregistreerde URL — alle drie fail-closed (403). Sporter: per koppeling "Oudere activiteiten ophalen" (backfill) + sync-historie (`GET /api/connectors/:id/runs`, `POST /:id/backfill`). Admin: `GET /api/admin/sync-diagnostics` + sectie "Automatische datasync" (per provider runs/mislukt/deels, recente runs, webhook-statussen, mislukte webhooks). Test: `test:provider-sync` (15 checks, via shell).

- **Clubomgeving** (`/club`, `/club/beheer`) — uitgebreid (Opdracht 15): 11 rollen (o.a. hoofdtrainer/assistent/mechanieker/vrijwilliger/alleen_lezen), clubstatus actief/beperkt/geschorst/beeindigd (niet-actief ⇒ alle schrijfacties 409 via `clubWritableOr409`, bekijken blijft), modules-toggles, club-/teamcode aansluiten (`POST /api/clubs/join`, DB-unieke codes, transactie + advisory lock per club tegen overboeken; QR in beheer), locaties-CRUD, samengevoegde clubkalender, aanmeldstatus "misschien" (telt geen plek), aanwezigheid "te_laat", per-scope consent (9 categorieën; UI leidt "aan" af uit consent-rijen met status granted); trainer-samenvatting toont consentScopes + audit `sportdata_ingezien`. — eigen club-entiteit met rollen owner/admin/trainer/teammanager/parent/member (schema `lib/db/src/schema/club.ts`, rechtenlaag `lib/club-permissions.ts`, routes `routes/club.ts`, invitations club-branch). Least privilege: beheer ziet NOOIT sportdata; trainers alleen expliciet (team/groep) toegewezen sporters mét consent; jeugd (<16 of onbekende leeftijd, fail-closed) alleen via gekoppelde ouder. Clubtrainingen: aanmelden/afmelden/reserve (vol → reserve; afmelden promoveert oudste reserve + notificatie; row-lock FOR UPDATE tegen overboeken); schema-koppeling maakt NIEUWE planned_workouts-rij source="club" — coachtraining vervangen geeft 409, wordt nooit automatisch overschreven. Wedstrijden + beschikbaarheid, berichten (club/team/groep-scopes), pakketten proef/start/basis/groei met limieten die óók bij invite-accept gelden, export zonder sportdata, append-only audit. Frontend `pages/club.tsx` + `pages/club-beheer.tsx` + `hooks/use-club.ts`. Test: `test:club` (16 scenario's, via shell).

- **Sociale omgeving & profielprivacy** (`/samen` + `/profiel/:clerkId`) — netwerk-top op Samen (tellingen vrienden/volgers/gevolgd + lijsten, ontvolgen), Mensen vinden (zoeken, sha256-contactmatching via `navigator.contacts` + `crypto.subtle` — server matcht alleen hashes van geregistreerde e-mails, max 500; uitnodigingslink via bestaande invitations `friend_athlete`), openbare profielpagina met server-side gefilterde projectie. Per-categorie privacy: 17 categorieën × 6 niveaus (iedereen/sparki/volgers/vrienden/begeleiders/alleen_ik) in `profile_privacy`; rechtenlaag `lib/profile-privacy.ts` is de ENIGE waarheid. **Fail-closed:** afgeschermd (profiel=alleen_ik), geblokkeerd en niet-bestaand zijn voor de kijker ononderscheidbaar (neutrale 404 / "Deze actie is nu niet mogelijk." / "Dit verzoek kan niet worden verstuurd."); zoeken en contactmatching verzwijgen afgeschermde/geblokkeerde profielen. Blokkeren verbreekt vriendschap + volgen beide kanten atomair (transactie); na een geweigerd verzoek mag alleen de weigeraar heropenen. Engine `engines/social/network.ts` (+ `index.ts`), routes `routes/social.ts`, frontend `pages/profiel.tsx`, `pages/samen.tsx` (NetwerkOverzicht/MensenVinden), `components/sparki/profile-privacy-grid.tsx` in instellingen. Test: `test:social-privacy` (22 scenario's, via shell).
- **Ouder-/verzorgeromgeving** (Golf 12) — per-categorie deelrechten (8 categorieën: gezondheid, herstel, slaap, planning, wedstrijd, aanwezigheid, voeding, communicatie) op de ouder-koppeling (`parent_athlete_links.permissions` + `ageTierAtConsent`/`consentConfirmedAt`). Rechtenlaag `lib/parent-permissions.ts` (`effectiveParentAccess`) is de ENIGE waarheid voor álle ouder-routes (ook legacy `/api/parent/athletes` + context): kill-switch `dataSharingParent=none` sluit alles; veiligheidsminimum = gezondheid+herstel; leeftijdstiers u16/16–17/volwassen — **onbekende leeftijd fail-closed op veiligheidsminimum**; tierwissel vereist herbevestiging (volwassen: alles dicht tot herbevestigd); onbevestigde keuze komt nooit boven safety-only; alleen bij u16 mag de ouder zelf rechten aanpassen. Ouderacties: ziek/afwezig-meldingen (`parent_reports`, ook zichtbaar voor coach), noodcontacten (max 5, atomair via advisory lock), bevestigingen (wedstrijd/aanwezigheid, idempotent), berichten (communicatie-recht beide kanten). Uitnodiging weigeren = atomair pending→declined, geen koppeling. Routes `routes/parent.ts` + sporterkant in `routes/links.ts`; frontend `parent-home.tsx` (op `/api/parent/overview`) + `links-section.tsx` (per-categorie toggles). Test: `test:parent-environment` (16 scenario's, via shell).

- **Releasegroepen & gecontroleerde uitrol** (Golf 14) — vier releasegroepen (intern/test/pilot/productie; `user_profiles.releaseGroup`, effectief = meest permissieve incl. clublidmaatschap, onbekend = productie fail-closed). Featureflags per groep/platform/rol + deterministisch rollout-% (`lib/flags.ts`, `rolloutBucket` stabiel per gebruiker+flag); kill switches per domein (`lib/kill-switches.ts` + `killSwitchGuard` vóór routers ⇒ 503 met eerlijke Nederlandse melding); versiecheck via headers `x-sparki-app-version`/`x-sparki-platform` (`lib/version-gate.ts`, te oud ⇒ 426 + blokkeerscherm web én mobiel; de 426-boodschap is gelatcht in module-state zodat een laat gemount scherm de blokkade altijd overneemt). Foutregistratie (`lib/error-registry.ts`, `POST /api/release/errors`, rate-limited, ook zonder sessie): groepering op fingerprint (platform+genormaliseerde melding+stack-top), ernst alleen omhoog. **Uitrolbewaking (auto-stop) telt uitsluitend geauthenticeerde kritieke voorvallen mét bijpassende `flagKey` — nooit een globale teller, en anonieme meldingen worden server-side gedowngraded naar "fout"** (misbruikbescherming). Beheerbord `/admin` → releasebeheer (`release-admin.tsx`, 8 tabs: overzicht/groepen/flags/switches/versies/fouten/pilot/berichten); releaseberichten alleen rustig op Vandaag (`release-note-card.tsx`); pilotconsent; audit op alle beheeracties. Mobiel: `lib/release.ts` (headers, 426-listener, ErrorUtils-handler → foutregistratie) + `VersionBlockScreen`. Gedeelde client: `custom-fetch.ts` `setDefaultHeaders`/`setErrorStatusHandler`. Test: `test:rollout` (11 scenario's, via shell).

- **Store-distributie (Golf 28)** — App Store/Google Play-gereedheid. Publieke juridische pagina's `/privacy` + `/voorwaarden` (`pages/legal.tsx`, veilige minimale markdown-renderer, zelfde bron als in-app via `/api/legal/:kind`). Store-register `docs/store/store-listing.json` (NL beschrijvingen, machtigingsuitleg, dataveiligheid, screenshotformaten — placeholders `<productiedomein>` vóór indiening vervangen). Versiecontrole uitgebreid: additief `version_requirements.recommended_version`; `GET /api/release/version-check` geeft rustig `updateAdvies` (nooit blokkade); mobiel toont wegtikbaar update-kaartje op het startscherm. **426-blokkade wordt tijdens een actieve rit uitgesteld** (`setRideActive` in `lib/release.ts`, gekoppeld aan `useRideRecorder` start/stop/reset; melding volgt na de rit). Distributiekanaal (eas.json `EXPO_PUBLIC_CHANNEL`) gaat mee als header `x-sparki-kanaal` en werkt als PLAFOND op de releasegroep (`channelCap` in `lib/release-groups.ts`: onbekend kanaal ⇒ productie fail-closed; geen header = geen plafond; best-effort — client-header, geen server-attestatie). Controles: `pnpm --filter @workspace/sparki-mobile run check:prod-config` (app.json/eas.json/secrets) + `node scripts/store-release-check.mjs` (repo-breed + externe checklist: ondertekening via EAS, store-formulieren, testaccount, version_requirements bijwerken per release). Test: `test:store-release` (14 checks, via shell).

- **Releasecandidate (Golf 29)** — volledige releasestraat groen op commit `bb2391f` (22-07-2026, 112 fasen incl. nieuwe `storecontrole:*`-fasen). Driftcontrole kent nu drie catalogus-geverifieerde no-op-lussen (63-tekens-afkapping; array-default incl. `'{}'::text[]`-cast; gelijknamige UNIQUE ADD+DROP-churn) — regressie: `node scripts/test-check-schema-drift.mjs` (11 gevallen). RC-bewijs: `docs/RELEASE_ACCEPTANCE.md` + `docs/release-check-latest.json`; feitelijke stand + statuslabels in `docs/SPARKI_CURRENT_STATE.md` §11b.

- **Routeplanner-hub & routevoorstellen** — `/routes` is een hub met vier keuzes (Route laten maken / GPX importeren / Bewaarde routes / Navigatie-instellingen, via `?view=`); deep-links `nav`/`ritopties`/`route` openen bewaard. Navigatie-instellingen (`/api/nav-settings`, `nav-settings-panel.tsx`) sturen datavelden (max 4), lettergrootte, balkpositie, kaartoriëntatie en auto-weergaven; expliciete startmenu-keuzes winnen altijd (`rideOptionsExplicit`). Routevoorstellen tussen fietsmaatjes (`route_proposals`, `routes/route-proposals.ts` gemount vóór de hoofd-routesrouter): voorstellen alleen aan geaccepteerde vrienden, accepteren kopieert de route atomair (één transactie), aanpassen maakt altijd een NIEUWE route (echte hergegenereerde lus of eerlijk 503). UI: Delen-menu "Stel voor aan fietsmaatje" + voorstellen-inbox in bewaard. Test: `test:route-proposal` (10 scenario's, via shell). **Live nav-audio & waypoint-eerlijkheid:** waypoints zijn vormgevers, géén bestemmingen — server saneert nav-stappen (`lib/routing/nav-sanitize.ts`, toegepast in ors.ts + alle read-paths; alleen eerste Vertrek + laatste Aankomst blijven; via-tussenstop = stille regel "Tussenstop"). Mobiel: pure cue-engine `lib/nav-cues.ts` (snelheidsafhankelijke timing, dedupe per stap:fase, off-route 1× per episode) + `lib/nav-audio.ts` (expo-audio-tonen + expo-speech, respecteert stil/volume: `playsInSilentMode:false`, achtergrond aan) + HUD-toggles die `soundCues`/`voiceCues` in `/api/nav-settings` delen met web (`nav-settings-panel.tsx`; afwezig=true). Tests: `test:nav-cues` (mobiel, 9) + `test:nav-sanitize` (api-server, 6, via shell); handmatig protocol in `docs/mobile-testprotocol.md` §4.5–4.8.
- **Mobiel nav-HUD & van-route-keuze** — navigatiescherm: overlays (klimprofielkaart uit ECHTE routeklimdata, anders eerlijk afwezig) krimpen de kaart via pure lay-outkern `lib/nav-layout.ts` (`computeNavLayout`, nooit overlap, herstel bij sluiten); kaart vrij te pinchen/pannen/roteren zonder terugspringen (`lib/map-camera.ts`: geen `animateCamera` buiten volgmodus), knop "Terug naar mijn positie" herstelt volgen; HUD-metrics begrensd via `lib/hud-metrics.ts` (`chooseMetricLayout` zet de eenheid onder de waarde bij grote getallen/systeemletters). Van de route: kaart "Je bent van de route afgeweken." met drie keuzes (snelste weg terug / opnieuw naar bestemming / negeren) uit `lib/off-route-choice.ts` — één kaart per episode, negeren blijft stil tot de afwijking wezenlijk groeit, rejoin-verzoeken gegate op afkoeltijd/verplaatsing (geen herberekenlus); wedstrijdroutes (usageType `wedstrijd`) zetten terugkeren voorop en de route wordt NOOIT automatisch vervangen (omleiding blijft client-only overlay; backend rejoin-mode `bestemming` mikt op het route-einde). Tests: `test:nav-hud` (9) + `test:off-route-choice` (11), via shell.

- **Wedstrijdflow & Race Intelligence** (Golf 16) — één flow op `/races`: toevoegen/importeren → info (categorie, inschrijving niet_ingeschreven/ingeschreven/bevestigd, doel, route-koppeling met eigendomscheck, status gepland/geannuleerd) → parcoursanalyse (`lib/race-course.ts`, feiten met soort feit/afgeleid/inschatting/ontbreekt uit gekoppelde route + racevelden, eerlijke gaten met gerichte vervolgvraag) → advies (`lib/race-advice.ts`, typologie feit/regel/inschatting/coachinstructie — coachinstructie ALTIJD letterlijk en bovenaan; notPossible-lijst voor eerlijk ontbrekend advies) → dossier (`GET /api/races/:id/dossier` via `lib/race-dossier.ts`, fase aankomend/racedag/afgerond/geannuleerd). Annulering telt nergens mee (plan/doelen/context/coach-signalen/herinneringen filteren geannuleerd; evaluatie niet-evalueerbaar; Journey toont "Geannuleerd" zonder uitslag). Frontend: nieuwe velden in RaceForm + `RaceWerkbladPanel` (parcoursfeiten + advies met soort-tags, coach-accent) in de bewerkweergave. Test: `test:race-flow` (12 scenario's, via shell met `DEV_AUTH_BYPASS=true`).

- **Gezondheids- & herstelflow** (Golf 26) — één samenhangende flow ziek/blessure/pijn → statussync → verloop → hervatten → opbouwvenster. Engine `lib/health-flow.ts`, routes `routes/health-flow.ts` (`/api/health-flow`): klachten (`health_complaints` + `health_complaint_updates`, kind ziekte/blessure/pijn, ernst, trainingsimpact geen/aangepast/niet_trainen, bron incl. medisch_bevestigd + letterlijke instructie), statussync raises-only (ziekte>blessure); "hersteld" kan NOOIT via een tussentijdse update (400) — alleen via de expliciete hervattingsstap (`POST /resume`, 409 bij actieve niet_trainen-klacht), die klachten sluit, status ok zet en een opbouwvenster start (dag 1–7, loadFactor < 1). Noodinformatie (`health_safety_info`) standaard NIET gedeeld. Dagelijkse check-in (`/checkin-context`) vraagt alleen wat vandaag ontbreekt (feel/vermoeidheid/slaap/spierpijn/stress 1–5, `athlete_daily_metrics` uitgebreid met soreness/stress). Doorwerking: readiness, coach-signalen, race-advies, ouder-samenvatting, val-alarm safetyLine, mobiele waarschuwing vóór "Start rit". Frontend: `/lichaam` (`health-flow-section.tsx` + `checkin-sheet.tsx`); `HealthStatusControl` blijft de snelle zet op dag-homes. Test: `test:health-flow` (14 scenario's, via shell).
- **AI-helpdesk & supportautomatisering** (Golf 27, `/support`) — deterministische supportlaag: engine `lib/support/helpdesk.ts` (classificatie via Nederlandse sleutelwoord-stammen `stam\w*` — nooit een kale stam vóór `\b`; expliciete categoriekeuze wint), antwoordbesluit-matrix direct/beperkt/storing_bekend/meer_info/mens; gevoelige onderwerpen (privacy, accountverwijdering, betaling, gezondheid/veiligheid, minderjarig — onbekende leeftijd fail-closed) gaan ALTIJD naar een mens en `humanSendRequired` blokkeert auto-verzenden in beheer hard. Modelantwoorden (gateway-doel "helpdesk") verwoorden uitsluitend aangeleverde bronnen en geven eerlijk null — nooit verzonnen antwoorden. Tickets: `createOrAttachTicket` is race-veilig (één transactie + `pg_advisory_xact_lock` per gebruiker+dedupe-sleutel) — parallelle vragen over dezelfde storing/foutgroep leveren exact één open ticket (attach i.p.v. duplicaat). Schema `lib/db/src/schema/support.ts` (tickets/berichten/artikelen/bekende storingen/feedback, additief); routes `routes/support.ts` (`/api/support`: ask/feedback/tickets + beheer wachtrij/concept/verzend/samenvoegen/storingen/artikelen met audits en meldingen; interne notities/concepten nooit zichtbaar voor gebruikers, cross-account 404). Artikelen rolgebonden; opgelost ticket → artikel (onvoltooid = 409), publiceren bumpt versie. Frontend `pages/support.tsx` + `hooks/use-support.ts` + `support-admin.tsx` (/admin) + mobiel `app/(app)/support.tsx`. Test: `test:support-helpdesk` (21 scenario's incl. concurrency-regressie, via shell).

- **Planbeheer & eerlijke lege staten (mock-opschoning)** — alle QA/seed-rijen van de dev-bypass gebruiker zijn verwijderd; lege accounts tonen overal eerlijke lege staten (geen verzonnen sessies/CTL/TSS/plan). Het schema toont metadata (naam, aanmaakdatum, bron autonoom/advies, maker, doelstelling — eerlijk "Geen wedstrijddoel vastgelegd" zonder wedstrijd —, periode, status) met pauzeren/hervatten/verwijderen in `training-plan-panel.tsx` (PlanMetaCard). Levenscyclus-routes (`POST /api/training-plan/pause|resume`, `DELETE /api/training-plan`) zijn gescoped op ÉÉN huidig plan via `resolveCurrentPlan` (nieuwste actieve, anders nieuwste gepauzeerde — zelfde regel als de weergave); hervatten archiveert andere gepauzeerde plannen (nooit twee actieve); verwijderen wist plan_days + nog-geplande workouts, koppelt voltooide los en raakt `training_sessions` NOOIT. Test: `test:plan-lifecycle` (13 scenario's, via shell).
- **Trainingsplanning & adaptieve coaching** (Golf 23) — de keten doel→plan→uitvoering→feedback→voorstel is rond. Uitvoeringskoppeling (`lib/workout-execution.ts`): een echte activiteit koppelt aan de geplande training van dezelfde dag (auto bij ingest, race-veilig — handmatige koppeling wint) met deterministisch oordeel 70–130% marge ⇒ completed/partial/adjusted; zonder doelwaarden eerlijk "geen doelwaarden". Verlopen geplande trainingen worden lui op het leespad "missed". Statussen: planned/modified/completed/skipped/partial/adjusted/missed/cancelled; `planned_workout_changes` is append-only historie; annuleren = zachte statuswissel (DELETE), coachtrainingen 403 voor inhoud én annuleren (koppelen/status blijft toegestaan). Voorstellen: `lib/adjust-rules.ts` (`decideAdjustment`) beslist deterministisch server-side (pijn/moe⇒herstel, te zwaar⇒lichter, te licht⇒+15% TSS/+10% duur, verplaatsen/gemist⇒dag erna) met basis[] + confidence; het model verwoordt alleen {title,message} (Nederlandse fallback). Feedback draagt rpe/completion/deviationReason; "done"+completion≠volledig telt NIET als volgens-plan in het begeleidingsprofiel. Frontend: `workout-detail-drawer.tsx` (RPE-picker, voltooiing, voorstel met ZEKERHEID% + waarom, secties Uitvoering (koppel/ontkoppel zelfde-dag activiteiten) en Historie & beheer). Tests: `test:feedback-adjust` (11) + `test:plan-execution` (11), via shell.
- **Performance Lab** (Golf 22, `/lab`) — bestaand Lab afgerond op SSOT + eerlijkheid. Belasting: `GET /api/athlete/load` gebruikt de gedeelde `computeLoadSeries` (lib/recovery-load; 90d pre-warm, zelfde-dag-TSS één keer geteld, `?days=` geklampt 7–90, default 42) — nooit meer een tweede inline EWMA. Radar: pure `lib/performance-radar.ts` (6 assen; onmeetbaar ⇒ `level:null`+reden, nooit een neutrale 0.5; Vermogen vereist FTP én gewicht, W/kg 2.0–5.5; Gevoel ≥2 scores/28d; Regelmaat 28d/12); UI tekent pas bij ≥3 meetbare assen en toont anders een "nog niet meetbaar"-lijst. Periodefilter 14/30/90 dagen voor readiness/HRV; FTP-sectie leidt met `profile.ftp` (Sportpaspoort-SSOT); UitlegDots op secties 01–04 (4 nieuwe uitleg-keys). Tests: `test:performance-radar` (sparki, 7 scenario's) + `test:athlete-load` (api-server, 6 scenario's incl. cross-user-isolatie), via shell.

- **Routeketen & routebibliotheek** (Golf 19) — complete keten zoeken/maken → analyseren → bewaren/delen → koppelen → navigeren → vergelijken. Additief schema: `routes.favorite/version/deleted_at`, `route_shares` (unique nullsNotDistinct — idempotent delen met coach/club/team/persoon), `route_version_usages` (snapshot naam+versie per training/wedstrijd/activiteit/navigatie). API (`routes/routes.ts`): bibliotheek `GET /api/routes?q&scope(mijn/favoriet/wedstrijd/archief)&sort`, `/gedeeld` (ontvangerslijst), kijkersweergave voor gedeelde routes (nav/waypoints/profiel = null, vereenvoudigde geometrie + privacyNote; niet-gerechtigde = 404 fail-closed), PUT bump't versie alleen bij inhoudelijke wijziging (favoriet/status niet), duplicate (versie 1), delen-CRUD, `POST /:id/navigatie-start` (mobiel meldt gebruikte versie, idempotent), `GET /:id/vergelijk?importId=` (plan vs. echt gereden GPX-track uit `parsedSummary.route.geometry`: dekking%, afwijkingen, afstand/hoogte-verschil, gemiste meetpunten; geen track ⇒ 422). Races met routeId registreren versiegebruik "wedstrijd". DELETE: zacht (deletedAt) bij historie, anders echt weg. Frontend: `route-library.tsx` op `/routes` (tabs, zoeken, favoriet, delen, vergelijken); mobiel meldt navigatie-start. Test: `test:route-chain` (17 scenario's, via shell).

- **Sportpaspoort** (Golf 20, `/you` → sectie "Sportpaspoort") — herleidbaar sportprofiel BOVENOP `athlete_profiles` (SSOT blijft; geen parallel waardenstelsel). Additief schema `lib/db/src/schema/passport.ts`: `passport_value_events` (oud→nieuw, herkomst gemeten/handmatig/berekend/geschat, bron, actor, datum) + `passport_proposals` (partiële unique: één OPEN voorstel per sporter+veld). Engine `lib/passport.ts`, routes `routes/passport.ts` (`/api/passport`, alleen zelf; voorstel-besluit ook door gekoppelde coach; club heeft GEEN toegang). **Kerninvariant: waarde + event altijd in ÉÉN transactie** (`applyValueChange`/`recordValueEvent` met tx-parameter) — profiel-PUT, gezondheidsstatus en FTP-ondergrens-engine zijn zo geïnstrumenteerd; nooit best-effort. Zones-rakende autowijzigingen (niet-geschatte FTP) worden een VOORSTEL, nooit stil toegepast; geschatte FTP-ophoging blijft toegestaan maar krijgt een "berekend"-event; accepteren van een berekend FTP-voorstel houdt `ftpEstimated=true`. Ontwikkelingsweergave alleen bij voldoende meetpunten (≥2 FTP / ≥5 gewichten / ≥8 sessies), anders eerlijk "onvoldoende". Export door de sporter samengesteld; gezondheid/locatie/notities standaard UIT, lege selectie = 400. Frontend `components/sparki/sport-passport.tsx` + `hooks/use-passport.ts`. Test: `test:sportpaspoort` (16 scenario's, via shell).
- **Kennisbank (governed kennislaag)** (Golf 21) — één beheerde kennislaag, additief op bestaande lagen. Schema `lib/db/src/schema/knowledge-governance.ts`: `managed_knowledge_items` (domein/discipline/doelgroep/bron/betrouwbaarheid, levenscyclus concept→actief→verouderd/ingetrokken), `managed_knowledge_versions` (snapshot per publicatie), `knowledge_usage_events` (welke engine welk item+versie gebruikte, versie gepind), `knowledge_feedback` ("Klopt dit niet?"); `document_analyses.source_pages` (eerlijke paginanummers per veld, null als onzeker). Engine `lib/knowledge/governance.ts`: publiceren bumpt versie in transactie (FOR UPDATE) + snapshot; wijzigen van actief item zet terug naar concept (nooit stil wijzigen); ingetrokken is definitief (409). `getActiveKnowledge` filtert domein/discipline/doelgroep; `knowledgeSourceBlock` injecteert gepubliceerde kennis letterlijk in prompts (brief `routes/ai.ts`, fuelingplan `routes/nutrition.ts`); wedstrijdadvies (`routes/races.ts` → `buildRaceAdvice(..., managedItems)`) neemt items op als "feit"-adviezen (domein `vakkennis`, brontekst letterlijk, bron in de basis). Beheer: `/api/knowledge-beheer` (admin-only, `isAdmin`) + `KennisbankAdminSection` op `/admin` (CRUD, publiceer, verouderd/intrekken, conflicten = >1 actief zelfde domein+onderwerp, stale >365d, gebruik, feedback afhandelen). Publiek: `GET /api/knowledge/bronnen?topic=` + `POST /api/knowledge/feedback` (requireAuth); frontend `bron-vermelding.tsx` (BronVermelding) en UitlegDot haalt bronnen per uitleg-key. Test: `test:kennisbank` (14 scenario's, via shell).

- **Contextuele aandacht & meldingen** (Golf 24) — centrale meldingslaag `lib/notifications.ts`: elk type hoort bij precies één categorie (`TYPE_CATEGORY`); kritieke categorieën privacy/veiligheid zijn NOOIT uitschakelbaar. Meldingen dragen category/source/audience/expiresAt/resolutionKey/dedupeKey; `resolutionKey` = één open situatie ⇒ één melding, `resolveNotifications` laat rijen verdwijnen uit bel/tellers (historie blijft). Leespad filtert verlopen+opgeloste rijen (`activeNotificationFilter`). Voorkeuren (`engines/reminders/preferences.ts`): kanalen push/in-app/e-mail, stille uren (Europe/Amsterdam, ook over middernacht — dempen alleen push+e-mail voor niet-kritiek; kritieke push passeert altijd, kritieke e-mail volgt de e-mailschakelaar), categorie-toggles coach/club/sociaal/materiaal/sync; `deliver.ts` handhaaft `allowsCategory`+`channelAllowed` (e-mail in stille uren wordt later opnieuw geprobeerd, sentAt blijft NULL). Producenten: sync-fout ⇒ `sync:<providerId>` (resolve bij herstel), materiaal-nudge ⇒ `materiaal:<cat>` (resolve bij analyse), herbevestiging ⇒ `herbevestiging:<athleteId>` (resolve bij reconfirm). Instellingen-UI in `reminder-settings.tsx` (kanalen, stille-urenvenster met tijdvelden, categorieën). Mobiel toont tijdens rit/navigatie alleen de veiligheidskaart. Test: `test:attention-notifications` (8 scenario's, via shell).

- **Centrale AI-gateway (Golf 25)** — élke modelaanroep loopt via `aiMessage(purpose, clerkId, params)` in `artifacts/api-server/src/lib/ai/gateway.ts`. Doelenregister `AI_PURPOSES` (16 doelen: label/model/promptversie/inputcategorieën/toestemmingssoort/gevoelig/jeugdblokkade/timeout/maxRetries 0|1). Poorten per aanroep: kill switch `ai_processing` → toestemming per doel (ai_memory-toggle of gevoelige-toggle via `getEffectivePrivacy`, per aanroep gelezen dus intrekken werkt direct, fail-closed) → jeugdblokkade (`minorBlocked`, onbekende leeftijd = minderjarig) → redactie (sleutels/tokens/e-mail) → in-flight-dedupe (`dedupeKey`) → timeout + max 1 retry → metadata-logging in `ai_call_logs` (lib/db/src/schema/ai-gateway.ts; NOOIT prompt-/antwoordinhoud, wel tokens/latency/kostenindicatie/status). Helpers: `UPLOAD_DATA_RULE` (injectiehek voor document/foto/artikel-prompts), `expectJsonObject`/`limitText` (uitvoervalidatie), `recordFallbackUsed`/`recordRejectedOutput`, `__setAiTransportForTests`. Fouten: `AiBlockedError` (bewust niet uitgevoerd) / `AiUnavailableError` (provider faalde) — call sites vertalen naar eerlijke fallback. Beheer: `GET /api/admin/ai-insights` + sectie "Sparki-denkkracht (gateway)" op `/admin` (register + gebruik + recente problemen). Test: `test:ai-gateway` (14 scenario's, via shell).
- **Vrienden live op de kaart (Opdracht 4)** — opt-in live locatie delen tijdens mobiele navigatie, standaard UIT en per sessie. Backend: schema `lib/db/src/schema/live-location.ts` (sessions/grants/positions — één positierij per sessie, GEEN geschiedenis; bij stoppen wordt de rij verwijderd), engine `artifacts/api-server/src/lib/live-location/` (eerlijke veroudering: Live ≤20s → "x geleden" → ≥5 min geen coördinaten → ≥15 min weg; idle-verval 30 min geankerd op de laatste positie), routes `routes/live-location.ts` (`/api/live-location`): delen met geaccepteerde vrienden (whitelist, fail-closed 400) of een groepsrit van vandaag waarvoor je bent aangemeld (`/group-options`). Autorisatie wordt bij ELKE lezing opnieuw gecontroleerd (vriendschap beëindigd of groepsdeelname weg = positie direct weg); minderjarig/onbekende leeftijd deelt in een groep fail-closed alleen met geaccepteerde vrienden/begeleiders — óók her-gecontroleerd op leesmoment. Mobiel: `lib/live-share.ts` (adaptief interval via stilstand/scherm (AppState)/batterij (expo-battery)/offline — offline = niets sturen of bufferen; clustering; lokale herveroudering), `lib/live-share-api.ts`, kleine initialen-markers in `RouteMap.tsx`, deelkeuze + vrienddetail + auto-stop bij schermverlaten in `navigate/[id].tsx`. Tests: `test:live-location` (api-server, 21 scenario's, via shell) + `test:live-share` (mobiel, 10).
