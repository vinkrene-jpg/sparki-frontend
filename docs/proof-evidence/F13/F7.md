# F7 — Communicatie met bijlagen — F13-bewijsbundel

**Toets-SHA (vast):** `a2e08175` (huidige HEAD, hertoetst 2026-08-02)
**Bindend document:** `attached_assets/F7_COMMUNICATIE_MET_BIJLAGEN_(1)_1785682570230.md`
**Testrun:** `pnpm run test:club-communicatie-bijlagen` (api-server, `--dev-auth`, dev-API-header `x-dev-clerk-id`)
**Hertoets-log (na herstel):** `docs/proof-evidence/F13/logs/f7-club-communicatie-bijlagen-hertoets.log` — **20/20 groen** ("✅ F7 communicatie met bijlagen — 20 controles geslaagd").
**Oorspronkelijke log (vóór herstel, 12/20):** `docs/proof-evidence/F13/logs/f7-club-communicatie-bijlagen.log`.

> Alle onderstaande uitkomsten zijn OPNIEUW GEDRAAID op SHA `a2e08175` (+ herstel). Geen hergebruik van ouder bewijs.

## Uitkomst van deze run (na herstel — FIN-07/08)

**Volledige suite: 20/20 GESLAAGD.** De eerdere twee open punten (F7-01 en F7-02)
zijn hersteld én hertoetst.

**Herstel (F7-01/02).** `routes/storage.ts` weigert F7-berichtbijlagen
(retentiecategorie `club_message`) én F8-clubdocumenten (`club_document`) nu
ALTIJD fail-closed **404** op de generieke storage-route — óók voor de eigenaar
en óók na intrekking (geen 410-onthulling via de rauwe object-route). Het EIGEN
F7-serve-pad blijft bij intrekking **410** geven (expliciet "ingetrokken"). Door
dit herstel loopt de harness door: het eerder blokkerende scenario 13 is nu groen
en de scenario's 14–20 (groep één-richting, ongevraagd contact <16, ouder leest
mee, cross-link file-toegang, retentie-opruiming) zijn nu allemaal gedraaid en
geslaagd.

## Acceptatiecriteria (8 uit de spec, §Acceptatiecriteria) → scenario + uitkomst

| # | Criterium (letterlijk) | Scenario (gewone taal) | Uitkomst |
|---|---|---|---|
| 1 | Een clubbeheerder verstuurt een bericht met bijlage; de ontvangers zien het en kunnen de bijlage openen. | Clubbeheer plaatst een clubbericht met een afbeelding-bijlage; het clublid haalt het bericht op en downloadt de bijlage. | **GESLAAGD** — `✓ clubbeheer plaatst clubbericht met afbeelding-bijlage`, `✓ ontvanger ziet bericht + bijlage en kan downloaden` |
| 2 | Een gebruiker zonder recht op het bericht kan de bijlage niet zien én niet downloaden, ook niet via een directe aanroep met de bestands-id. | Een niet-lid probeert de bijlage te openen én rechtstreeks met de attachment-id op te vragen. | **GESLAAGD** — `✓ onbevoegde (niet-lid) kan bijlage niet downloaden, ook niet met directe id` |
| 3 | Een ingetrokken bestand is niet meer downloadbaar, ook niet via een link die eerder werkte. | Beheer trekt de bijlage in; het lid probeert de eerder werkende downloadlink; ook de rauwe object-URL via de generieke route wordt geprobeerd (als eigenaar). | **GESLAAGD** — `✓ ingetrokken bestand is niet meer downloadbaar (410), ook via oude link` (F7-serve-pad → 410) én `✓ na intrekken is óók de rauwe object-URL dood via de generieke storage-route (als eigenaar)` (generieke route → fail-closed 404). |
| 4 | Een geweigerd bestandstype geeft een duidelijke melding en wordt niet opgeslagen. | Upload van een uitvoerbaar/ongeldig type. | **GESLAAGD** — `✓ geweigerd type wordt niet opgeslagen + nette melding`, plus unit `✓ sniffer weigert uitvoerbaar bestand` |
| 5 | Een bestand dat zich voordoet als een toegestaan type maar dat niet is, wordt geweigerd op inhoud. | Upload van een ZIP met naam `.png`. | **GESLAAGD** — `✓ verkleed bestand (ZIP als .png) geweigerd op inhoud`, plus unit `✓ sniffer weigert verkleed ZIP-bestand` en `✓ sniffer herkent PNG op inhoud` |
| 6 | De pushmelding bevat noch de berichttekst noch de bestandsnaam. | Ontvanger krijgt een melding; body en titel worden gecontroleerd op inhoud/bestandsnaam. | **GESLAAGD** — `✓ pushmelding bevat geen berichttekst of bestandsnaam` (body = null, titel = "Nieuw bericht") |
| 7 | Gelezenstatus werkt per ontvanger. | Lid markeert bericht als gelezen; voor de afzender blijft het ongelezen. | **GESLAAGD** — `✓ gelezenstatus werkt per ontvanger` |
| 8 | De bewaartermijn van één jaar staat als configuratiewaarde en is niet hardcoded. | Retentietermijn wordt uit env gelezen (default 365), niet uit een vaste code-constante. | **GESLAAGD** — `✓ retentietermijn komt uit env (default 365), niet hardcoded` |

**Alle 8 acceptatiecriteria uit de spec: GESLAAGD.** Criterium 3 is bewezen via
zowel het F7-serve-pad (410, ook via oude link) als de generieke route
(fail-closed 404 na herstel).

## Aanvullende veiligheidsscenario's uit de spec (§harde grens <16, §rechten) — HERTOETST

Deze staan als testscenario's in dezelfde suite en draaiden na het herstel
allemaal groen (de harness liep door tot 20/20):

| Scenario (gewone taal) | Uitkomst |
|---|---|
| Groep-bericht van clubtrainer staat geen reacties toe (één richting). | **GESLAAGD** — `✓ groep-bericht staat geen reacties toe (één richting)` |
| Ongevraagd contact van volwassene naar jeugd <16 zonder koppeling wordt geweigerd (403). | **GESLAAGD** — `✓ ongevraagd contact zonder koppeling wordt geweigerd (403)` |
| Gekoppelde coach ↔ sporter <16: twee richtingen + ouder leest mee. | **GESLAAGD** — `✓ gekoppelde coach ↔ sporter<16: twee richtingen + ouder leest mee` |
| Een vreemde volwassene ziet het jeugd-gesprek niet. | **GESLAAGD** — `✓ een vreemde volwassene ziet het jeugd-gesprek niet` |
| Directe file-toegang cross-link/cross-club geweigerd op alle serve-paden. | **GESLAAGD** — `✓ directe file-toegang cross-link/cross-club geweigerd op alle serve-paden` |
| Retentie ruimt oude berichten op en trekt ALLEEN hun bijlagen in. | **GESLAAGD** — `✓ retentie ruimt oude berichten op, en trekt ALLEEN hun bijlagen in` |
| Generieke storage-route weigert F7-bestanden ook vóór intrekking (geen inline-omzeiling). | **GESLAAGD** — `✓ generieke storage-route weigert F7-bestanden ook vóór intrekking (geen inline-omzeiling)` (404) |

## Openstaande punten

Geen. De eerdere twee open punten zijn hersteld én hertoetst (FIN-07/08):

- **F7-01 (opgelost).** `routes/storage.ts` geeft nu voor F7-berichtbijlagen
  (retentiecategorie `club_message`) én F8-clubdocumenten (`club_document`)
  ALTIJD fail-closed **404** op de generieke storage-route — óók voor de eigenaar,
  óók na intrekking (geen 410-onthulling via de rauwe object-route). Het EIGEN
  F7-serve-pad blijft bij intrekking **410** geven ("expliciet ingetrokken").
  Test + route zijn nu uitgelijnd; scenario 13 groen.
- **F7-02 (opgelost).** Doordat scenario 13 slaagt, loopt de harness door; de zes
  <16-/rechten-/retentie-scenario's zijn nu deze SHA hertoetst en geslaagd
  (suite 20/20).
