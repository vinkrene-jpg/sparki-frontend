# F8 — Clubdocumenten — F13-bewijsbundel

**Toets-SHA (vast):** `a2e08175` (huidige HEAD, hertoetst 2026-08-02)
**Bindend document:** `attached_assets/F8_Clubdocumenten_Specificatie_1785682573916.md`
**Testrun:** `pnpm run test:club-documents` (api-server, `--dev-auth`, dev-API-header `x-dev-clerk-id`)
**Log:** `docs/proof-evidence/F13/logs/f8-club-documents.log`
**Uitkomst run:** **Alle 23 clubdocumenttests geslaagd** (exit 0), opnieuw gedraaid op SHA `a2e08175`.

## Acceptatiecriteria (5 uit de spec, §Acceptatiecriteria) → scenario + uitkomst

| # | Criterium (letterlijk) | Scenario (gewone taal) | Uitkomst |
|---|---|---|---|
| 1 | Publicatie van een document werkt en is zichtbaar voor de juiste rollen. | Beheer maakt een concept, publiceert versie 1; het clublid ziet het gepubliceerde document en downloadt de bytes. Een trainers/bestuur-document is zichtbaar voor de trainer maar niet voor een gewoon lid. | **GESLAAGD** — `✓ beheer publiceert versie 1 → zichtbaar voor lid`, `✓ gepubliceerde download door lid levert de bytes`, `✓ trainer ziet en downloadt het trainers_bestuur-document wél` |
| 2 | Versiewissel: oude versie blijft bewaard, nieuwe versie wordt de actieve. | Beheer maakt een nieuwe concept-versie (v1 blijft actief), publiceert v2; v2 wordt actief en v1 blijft bewaard. | **GESLAAGD** — `✓ nieuwe versie (concept) laat de actieve versie 1 ongemoeid`, `✓ versie 2 publiceren: v2 actief, v1 blijft bewaard` |
| 3 | Rolzichtbaarheid: onbevoegde rol ziet het document niet. | Een concept is onzichtbaar voor een lid; een trainers/bestuur-document staat niet in de lijst van een gewoon lid; een ouder krijgt 403 op een trainers/bestuur-document. | **GESLAAGD** — `✓ concept: lid ziet het document niet in de lijst`, `✓ gewoon lid ziet het trainers_bestuur-document NIET in de lijst`, `✓ ouder krijgt 403 bij het trainers_bestuur-document`, `✓ gekoppelde ouder kan het leden_en_ouders-document downloaden` |
| 4 | Bevoegde beheerder kan documenten beheren (aanmaken, wijzigen, publiceren). | Beheer/hoofdtrainer maakt, wijzigt titel/zichtbaarheid en verwijdert documenten (alle versies mee weg). Trainer en lid mogen NIET aanmaken/wijzigen/verwijderen. | **GESLAAGD** — `✓ beheer maakt een concept-document`, `✓ hoofdtrainer mág beheren`, `✓ beheer wijzigt titel/zichtbaarheid; lid mag niet`, `✓ verwijderen: lid 403, beheer ok (alle versies mee weg)`, `✓ trainer mag geen document maken (403)`, `✓ lid mag geen document maken (403)` |
| 5 | Geen toegang via directe API voor onbevoegden. | Directe download van een concept door een lid → 404 (niet lekken); directe download trainers/bestuur door gewoon lid → 404; niet-lid zonder ouderkoppeling → lijst 403, download 403; oude versie via versieId → 404. | **GESLAAGD** — `✓ concept: directe download door lid → 404 (niet lekken)`, `✓ gewoon lid krijgt 404 bij directe download van trainers_bestuur`, `✓ niet-lid zonder ouderkoppeling: lijst 403, download 403`, `✓ lid kan geen oude versie via versieId opvragen (404)` |

**Alle 5 acceptatiecriteria uit de spec: GESLAAGD.**

## Extra bewezen scenario's (dezelfde suite, ondersteunend)

- Documenttypen/categorieën uit §1 ondersteund: `leden_en_ouders`, `huisregels`,
  `trainers_bestuur` — `✓ beheer maakt een concept-document (leden_en_ouders)`,
  `✓ hoofdtrainer mág beheren (nieuwe categorie huisregels)`,
  `✓ beheer maakt en publiceert een trainers_bestuur-document`. Onbekende
  categorie → 400 (`✓ onbekende categorie is 400`).
- Zelfde veiligheidsprincipes als de rest van het platform: verkleed bestand
  (ZIP als `.pdf`) geweigerd op inhoud (`✓ verkleed bestand (ZIP als .pdf) wordt
  geweigerd`).
- Versiehistorie zichtbaar voor beheer (`✓ concept: beheer ziet het wél, met
  versiehistorie`) — dekt "duidelijke aanduiding van versie" (§4).

## Generieke-route-semantiek (na F7-herstel, geldt óók voor clubdocumenten)

Het F7-herstel in `routes/storage.ts` weigert naast F7-berichtbijlagen ook
F8-clubdocumenten (retentiecategorie `club_document`) nu ALTIJD fail-closed
**404** op de generieke storage-route (`/api/storage/objects/...`) — óók voor de
eigenaar en óók na intrekking (geen 410-onthulling via de rauwe object-route).
Het EIGEN F8-clubdocument-serve-pad blijft bij intrekking **410** geven. De vijf
acceptatiecriteria hierboven toetsen het F8-eigen serve-pad; de generieke route
is voor clubdocumenten dus dicht (404) en vormt geen omweg. Dit verandert de
uitkomst van de 23 clubdocumenttests niet.

## Openstaande punten

Geen. Alle acceptatiecriteria zijn deze SHA hertoetst en geslaagd (23/23).
