# F12 — Centrale inbox en notificaties — F13-bewijsbundel

**Toets-SHA (vast):** `a2e08175` (huidige HEAD).
De inbox-poort is opnieuw HERTOETST op deze SHA: `test:f12-inbox` opnieuw groen.
**Bindend document:** `attached_assets/F12_INBOX_EN_NOTIFICATIES_1785683562086.md`
**Bestaande bundel:** `docs/proof-evidence/F12/BEWIJS.md` (7 acceptatiecriteria, drempel/venster-onderbouwing, NOT-01..05)

## Testrun op SHA `a2e08175` (opnieuw gedraaid)

`pnpm run test:f12-inbox` → **14/14 scenario's geslaagd** (drempel=3, venster=24u), exit 0.
Log: `docs/proof-evidence/F13/logs/f12-inbox.log`.

> Toets-SHA is nu `a2e08175`. De inbox-poort uit `docs/proof-evidence/F12/BEWIJS.md`
> is hierboven OPNIEUW gedraaid op de huidige HEAD en opnieuw groen (14/14). De
> audience-scenario's doen een LIVE `fetch` naar de draaiende dev-API
> (`x-dev-clerk-id`); die passeerden op deze build.

## Acceptatiecriteria (7 uit de spec, via BEWIJS.md) → scenario + uitkomst

| # | Criterium | Scenario (gewone taal) | Uitkomst |
|---|---|---|---|
| 1 | Tien wijzigingen in hetzelfde object ⇒ één gebundelde melding. | 10× dezelfde bundleKey ⇒ exact 1 open rij, count=10; onder de drempel (2) ⇒ losse rijen. | **GESLAAGD** — `✅ bundeling: 3 of meer wijzigingen zelfde object ⇒ 1 bundel`, `✅ onder de drempel ⇒ losse meldingen`, `✅ bundeling: groei reset readAt` |
| 2 | Een melding opent de juiste rol en context. | `actionUrl` + `audience` sturen de rolwissel; de bundel-rij bewaart de laatste actionUrl. | **GESLAAGD (geverifieerd)** — bestaand pad in `notification-bell.tsx`; onderbouwd in BEWIJS.md §2 (geen UI-wijziging). |
| 3 | Gelezen is aantoonbaar iets anders dan afgehandeld. | `readAt` en `resolvedAt` zijn twee onafhankelijke kolommen, los zet-/filterbaar. | **GESLAAGD** — `✅ gelezen ≠ afgehandeld: readAt en resolvedAt los zetbaar` |
| 4 | Geen pushtekst bevat berichtinhoud/bestandsnaam/gezondheids- of prestatiegegeven. | Pushpayloads gecontroleerd: geen trainingstitel, geen wedstrijdnaam/locatie, geen providernaam/status/getallen (met gedocumenteerde life-safety-uitzondering val-alarm). | **GESLAAGD** — `✅ pushpayload bevat GEEN trainingstitel`, `✅ … GEEN wedstrijdnaam/locatie`, `✅ connection-health pushpayload bevat GEEN providernaam/status/getallen` |
| 5 | Stille uren gerespecteerd; urgente veiligheidsmelding komt er wél door. | Kritieke categorieën passeren stille uren; niet-kritiek respecteert alles. | **GESLAAGD** — `✅ kritieke categorieën worden NOOIT gebundeld`; stille-uren-pad ongewijzigd groen (BEWIJS.md verwijst naar `attention-notifications` 10/10). |
| 6 | Melding voor ingetrokken rol niet zichtbaar/actief, ook via directe aanroep. | Na rolverlies is de coach-melding onzichtbaar in `GET /notifications` én `PATCH /:id/read` geeft 404 (geen 403-lek), rij blijft ongelezen (fail-closed); audience-loos = altijd zichtbaar voor eigenaar. | **GESLAAGD** — `✅ audience: coach-melding onzichtbaar + niet-PATCHbaar na rolverlies`, `✅ audience-loos = altijd zichtbaar voor eigenaar` |
| 7 | Geen module voert nog een eigen meldingenlijst. | Alle producenten gaan door `createNotification`; coach-berichten/uitnodigingen zijn domeinlijsten, geen tweede meldingssysteem. | **GESLAAGD (geverifieerd)** — BEWIJS.md §NOT-02/04 (`rg createNotification`); reminders zijn een beargumenteerde uitzondering (1-per-object-per-venster, niets te bundelen). |

**Alle 7 acceptatiecriteria uit de spec: GESLAAGD.**

Aanvullend bewezen deze run (concurrency/beleid): `✅ concurrency: 10 parallelle
producenten ⇒ exact 1 bundel, count=10`, `✅ concurrency rond de drempel:
parallelle events verliezen niets`, `✅ beleid: al-GELEZEN losse rijen worden NIET
met terugwerkende kracht gevouwen`, `✅ config: drempel en venster
leesbaar/onderbouwd`.

## Openstaande punten

- **Beargumenteerde uitzonderingen (uit BEWIJS.md, ongewijzigd):** (1) val-alarm
  push (`routes/alerts.ts`) bevat bewust naam + laatst bekende locatie —
  life-safety `veiligheid`-melding; (2) reminders bundelen niet mee via
  `createNotification` (elke reminder is al 1-per-object-per-venster via unieke
  dedupeKey). Beide zijn gedocumenteerd en geen open gat.
- Deze bundel verwijst naar de volledige onderbouwing in
  `docs/proof-evidence/F12/BEWIJS.md`; de inbox-poort is hier opnieuw op de vaste
  SHA gedraaid.
