# F13 — Bewijsbundel F2 · Zichtbare context

**Toets-SHA (vast):** `a2e08175`
**Getoetst op:** 2026-08-02
**Spec:** `attached_assets/SPARKI_HERSTEL_EN_AANVULLING_01_1785657353898.md` — HA-06 t/m HA-09
**Poortvoorwaarde 2 (context):** op elk scherm is te benoemen wie je bent, waar
je bent, en of je naar echte of naar testgegevens kijkt.

---

## Wat dit is (FIN-04)

F2 gaat over één belofte: **op elk scherm zie je welke rol actief is, in welke
omgeving je zit, en of het test- of productiegegevens zijn.** De eindopdrachtgever
brak op 2 augustus een praktijktest af omdat dit niet vast te stellen was
(HA-08); F2 dicht dat.

---

## Bewijs A — contextregel met actieve rol (e2e, verse prod-build op a2e08175)

**Test:** `node e2e/tests/wp-f4-context.mjs`
**Log:** `logs/F2_wp-f4-context.log`
**Screenshot:** `e2e/evidence/wp-f4-context/context-athlete.png`
**Prod-build:** verse `artifacts/sparki/dist/public` op SHA **a2e08175**
(`logs/web-build.log` → `version.json` = `{"sha":"a2e08175", …}`). Deze e2e
gebruikt `startProdServer()`, die exact deze verse build serveert. Dit
bewijs is dus **hertoetst op a2e08175.**

| Scenario in gewone taal | Uitkomst | Toelichting |
|---|---|---|
| Ik open de sporter-home; de contextregel toont permanent mijn actieve rol (HA-06) | GESLAAGD | log: `[OK] contextregel zichtbaar met actieve rol — SPORTER`; testid `[data-testid="context-rol"]` toont "SPORTER" |
| De contextkiezer lekt geen aantallen/informatie uit niet-actieve contexten (alleen het rollabel) | GESLAAGD | log: `[OK] contextkiezer zonder aantallen uit andere contexten` |
| De rolwissel-knop (contextkiezer) is aanwezig in het menu | GEFAALD (test-drift) | zie open punt 1 |

**Codebewijs (HA-06/HA-07):**
- `artifacts/sparki/src/components/ds/context.tsx:26` — de contextregel rendert
  met `data-testid="context-rol"` (actieve rol permanent zichtbaar).
- `artifacts/sparki/src/components/sparki/main-menu.tsx:421–432` — de
  contextkiezer ("Context: <rol>", `title="Wissel van context"`) verschijnt
  zodra het account meer dan één context heeft (`contexts.length > 1`) en
  toont alléén het rollabel, geen aantallen.

---

## Bewijs B — test-/productiecontext onmiskenbaar onderscheiden (HA-07)

**Log:** `logs/F1_wp-r0-rollen.log` (banner-regels)
**Scenario:** in een testcontext toont elk scherm een banner die test
onmiskenbaar van productie onderscheidt.

Uitkomst: GESLAAGD (op web-SHA 857aec22, niet hertoetst op a2e08175). De banner
luidt letterlijk, per identiteit:
`TESTCONTEXT · DEV PREVIEW @ 857AEC22 · TESTFIXTURE CLUBBEHEERDER · ROL ATHLETE`
— dit combineert **wie** (identiteit + rol), **waar** (omgeving/SHA) en **welke
gegevens** (TESTCONTEXT) in één vaste regel, precies HA-09.

**Omgevingsmarker in de API (context "waar"):**
`GET /api/version` → `{"service":"api-server","environment":"development","commit":"a2e08175", …}` —
bevestigt op de toets-SHA dat de omgeving expliciet benoemd wordt.

---

## Uitkomst F2

- **Contextregel met actieve rol: GESLAAGD, hertoetst op a2e08175.**
- **Geen datalek uit niet-actieve contexten: GESLAAGD, hertoetst op a2e08175.**
- **Test/productie-onderscheid via banner: GESLAAGD op web-SHA 857aec22 (niet
  hertoetst op a2e08175); de omgevingsmarker in `/api/version` is wél op
  a2e08175 bevestigd.**

## Openstaande punten

1. **e2e-selector-drift, geen productgat.** `wp-f4-context.mjs:80` zoekt
   `button[title="Wissel van rol"]`, maar de knop heet in de code inmiddels
   `title="Wissel van context"` (`main-menu.tsx:428`). De rolwissel-knop
   bestaat dus wél; de test timeoutt op de verouderde selector. Bovendien
   rendert de kiezer alleen bij >1 context, wat afhangt van de account-setup in
   de test. Niet gefixt (F13 = alleen bewijs).
2. **Banner-bewijs (Bewijs B) draait tegen web-SHA 857aec22.** Voor een
   volledige hertoets op a2e08175 moet de web-preview op de toets-SHA draaien
   (verse build staat klaar op a2e08175). Niet zelf herstart i.v.m. de
   F13-regel "geen workflow-herstarts".
