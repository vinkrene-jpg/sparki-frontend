# F11 — Centrale bestands- en medialaag — F13-bewijsbundel

**Toets-SHA (vast):** `a2e08175` (huidige HEAD).
De onderliggende bewijsbundel is opnieuw HERTOETST op deze SHA: beide poorten
zijn op `a2e08175` opnieuw groen gedraaid.
**Bindend document:** `attached_assets/F11_Centrale_bestands_en_medialaag_Specificatie_1785683681784.md`
**Bestaande bundel:** `docs/proof-evidence/F11/BEWIJS.md` (criteria → bewijs, veiligheidsbeleid, review)

## Testruns op SHA `a2e08175` (opnieuw gedraaid)

| Testrun | Uitkomst | Log |
|---|---|---|
| `pnpm run test:f11-files` | **12/12 controles geslaagd** (exit 0) — `✅ F11 centrale bestands- en medialaag — 12 controles geslaagd` | `logs/f11-files.log` |
| `pnpm run test:f11-omlegging` | **11/11 controles geslaagd** (exit 0) — `✅ F11 module-omlegging — 11 controles geslaagd` | `logs/f11-omlegging.log` |

> Toets-SHA is nu `a2e08175`. De bundel `docs/proof-evidence/F11/BEWIJS.md` was
> eerder gedateerd 02-08-2026; de daarin genoemde poorten `test:f11-files 12/12`
> en `test:f11-omlegging 11/11` zijn hierboven OPNIEUW gedraaid op de huidige
> HEAD en opnieuw groen. Geen "bewijs van oudere SHA" meer voor deze twee poorten.

## Acceptatiecriteria → scenario + uitkomst (hertoetst op deze SHA)

| Criterium (spec, via BEWIJS.md) | Scenario (gewone taal) | Uitkomst |
|---|---|---|
| Upload, preview, download door bevoegde. | Eigenaar registreert een geldige afbeelding en downloadt die met nosniff + attachment-header. | **GESLAAGD** — `✓ eigenaar uploadt (register) een geldige afbeelding`, `✓ bevoegde (eigenaar) kan downloaden met nosniff + attachment` |
| Vervangen behoudt oude versie. | Een bestand wordt vervangen; de oude versie blijft downloadbaar. | **GESLAAGD** — `✓ vervangen zonder historieverlies: nieuwe versie + oude blijft downloadbaar` |
| Ingetrokken bestand niet meer downloadbaar, ook niet via oude link. | Intrekken → serve-route geeft 410, ook via de eerder werkende link; materiaalfoto-serve is fileId-aware (410). | **GESLAAGD** — `✓ ingetrokken bestand weigert op de serve-route (410), ook via oude link`, `✓ na intrekken valt ook de oude serve-link dicht (410)`, `✓ materiaalfoto-serve is fileId-aware: ingetrokken ⇒ 410` |
| Geweigerd type / te groot duidelijk afgewezen. | Verkeerd type → 415; te groot → 400; materiaalpoort weigert verkleed type (415). | **GESLAAGD** — `✓ verkeerd type wordt geweigerd (415) door scanFile`, `✓ te groot bestand wordt geweigerd (400) door scanFile`, `✓ materiaalfoto-poort weigert een verkleed type (415)` |
| Duplicaat op checksum herkend; dedupe-revoke doodt zusterrij niet. | Zelfde eigenaar + zelfde bytes hergebruikt het object; revoke van rij A laat levende zusterrij B leven; dedupe is niet cross-eigenaar. | **GESLAAGD** — `✓ dedupe: dezelfde eigenaar + dezelfde bytes hergebruikt het object`, `✓ dedupe is NIET cross-eigenaar (privacy)`, `✓ dedupe: revoke van rij A laat de levende rij B op /api/storage leven`, `✓ generieke storage-route: dedupe-revoke doodt de nog levende zusterrij NIET` |
| Onbevoegde kan niet zien/downloaden. | Onbevoegde krijgt 404 (nooit lekken) per route; onbevoegde op generieke route 404. | **GESLAAGD** — `✓ onbevoegde krijgt 404 (nooit lekken)`, `✓ centraal-beheerd object: onbevoegde krijgt 404 (geen lek)` |
| Veilige bestandsnaam. | Path-traversal-naam wordt gesaneerd; sniffer herkent PNG op inhoud en weigert verkleed ZIP. | **GESLAAGD** — `✓ path-traversal-bestandsnaam wordt gesaneerd`, `✓ sniffer herkent PNG op inhoud, weigert verkleed ZIP` |
| Module-omlegging: geen module een eigen uploadoplossing. | Journey-media, materiaal-poort, presign-finalisatie e.d. lopen door de centrale poort; andermans media/presign-object weigert (403/404); rauwe presign-bron gequarantaineerd na finalisatie. | **GESLAAGD** — `✓ centraal-beheerd object: eigenaar haalt op via de generieke route`, `✓ eigen journey-media verwijderen trekt de centrale files-rij in (revoke)`, `✓ andermans journey-media verwijderen faalt (404)`, `✓ registerFromObjectPath weigert andermans presign-object (403, geen takeover)`, `✓ rauwe presign-bron is na finalisatie gequarantaineerd` |

**Alle acceptatiecriteria uit de F11-spec: GESLAAGD (hertoetst op `a2e08175`).**

## Openstaande punten

- **Eerlijke beperkingen (uit BEWIJS.md, ongewijzigd):** video (Journey/race-room)
  blijft op presign zonder her-encoding; club-logo (SVG), CSV-ledenimport en
  GPX/FIT/TCX-import vallen buiten de medialaag; de bestaande `material`-routetest
  faalt 8/26 identiek mét en zónder F11 (AI-gateway-consent in testomgeving,
  voorbestaand). Zie `docs/proof-evidence/F11/BEWIJS.md` §"Eerlijke beperkingen".
- Deze bundel verwijst naar de volledige onderbouwing in
  `docs/proof-evidence/F11/BEWIJS.md`; alleen de twee kernpoorten zijn hier
  opnieuw op de vaste SHA gedraaid.
