{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build && node scripts/check-deploy-size.mjs --warn-only",
    "check:deploy-size": "node scripts/check-deploy-size.mjs",
    "check:sanity-5b": "node scripts/test-check-sanity-reports.mjs && node scripts/check-sanity-reports.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "playwright-core": "^1.62.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
