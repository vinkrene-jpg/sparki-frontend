// Web stub: react-native-maps has no working web build, so the ride-detail map
// is never rendered on web (the screen shows an honest fallback instead). This
// file keeps react-native-maps out of the web bundle entirely.
export function TrackMap(): null {
  return null;
}
