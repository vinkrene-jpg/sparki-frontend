// Web stub: react-native-maps has no working web build, so the map is never
// rendered on web (the navigate screen shows an honest fallback instead). This
// file keeps react-native-maps out of the web bundle entirely.
export function RouteMap(): null {
  return null;
}
